//désolée, si je décommente, je ne peux plus voir la page et cette page est une page grand public :P (Gen)

// import { redirect } from '@sveltejs/kit';

// export function load ({cookies}){
//     const role = cookies.get('role');
    
    // if(role != '3')
    //     redirect(302, '/');
//}